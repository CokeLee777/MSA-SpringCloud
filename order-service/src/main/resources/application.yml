# ?? ?? : 0(?? ?? ??)
server:
  port: 0

# ??? ?? ??
spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} # ???? ??? ??
    hostname: localhost
  client:
    register-with-eureka: true  # EUREKA Registry ? ?? ??
    fetch-registry: true        # EUREKA Registry ? ?????? ??? ???? ??
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

logging:
  level:
    com.example.orderservice: DEBUG